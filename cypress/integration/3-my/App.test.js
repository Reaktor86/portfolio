import QuizButton from "../../../src/pages/quiz/UI/QuizButton";
import {mount} from "@cypress/react";

describe('тест App', () => {

    it('должен загрузить компонент Main', () => {
        cy.visit("/");
        /*cy.get('.Portfolio').should('have.class', 'header');*/
    })

})
